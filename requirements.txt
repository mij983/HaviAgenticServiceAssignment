# Run: python install.py
# Do NOT run pip install -r requirements.txt directly
# because torch needs a special index URL for CPU-only version

sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
chromadb>=0.5.0
ollama>=0.2.0
pandas>=2.0.0
numpy>=1.26.0
pyyaml>=6.0.0

# KB document ingestion (optional but recommended)
pypdf>=4.0.0          # PDF support  -- pip install pypdf
beautifulsoup4>=4.12  # HTML support -- pip install beautifulsoup4

# RLHF — no extra packages needed (uses stdlib json + existing deps)

# NOTE: Embedding model changed from all-MiniLM-L6-v2 to all-mpnet-base-v2
# for better accuracy. This downloads ~420MB on first run (was ~90MB).
# If you already have a built KB with the old model, rebuild it:
#   python build_knowledge_base.py --rebuild
#   python build_knowledge_base_docs.py --rebuild-docs
